# ===================================
#  List Column Definitions
# ===================================

columns:
    instore:
        label: raktáron
        type: switch
        default: 1

    id:
        label: ID
        searchable: true
        readonly: true

    name:
        label: name
        sortable: true
        searchable: true
        type: text

    description:
        label: description
